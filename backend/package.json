{
  "name": "backend",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "prisma:generate": "bunx prisma generate",
    "prisma:deploy": "bunx prisma migrate deploy",
    "test": "bunx jest --config jest.config.mjs",
    "seed": "bun run prisma/seed.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.1",
    "jest": "^29.7.0",
    "prisma": "^6.3.1",
    "ts-jest": "^29.2.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1",
    "axios": "^1.7.9"
  }
}